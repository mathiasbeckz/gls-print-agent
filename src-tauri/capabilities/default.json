{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for GLS Print Agent",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
